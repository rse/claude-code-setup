---
argument-hint: "[query]"
description: "Query Brave Search"
---

Query Brave (search.brave.com) with the `brave_web_search` MCP tool for the following query:

$ARGUMENTS

Then summarize the results.

